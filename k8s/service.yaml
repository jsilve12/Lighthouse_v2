apiVersion: v1
kind: Service
metadata:
  name: lighthouse-api
  namespace: lighthouse
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: lighthouse
    app.kubernetes.io/component: api
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
